var our_CurrentTime = 0;

YOUTUBE_READY = 1;

function react_to_video(){
	our_CurrentTime += delta_t;
}

console.log('Type "ChangeScene(x)" with desired chapter in place of x to see different models')
THREE.TextureLoader.prototype.crossOrigin = '';

function loadpic(url, type, index) {
	//these lines are for if you have no internet
	pictures_loaded++;
	if(pictures_loaded === virus_textures.length + random_textures.length + slide_textures.length ) {
		bind_pictures();
	}
}

init();
load_AV_stuff();
